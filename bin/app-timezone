#!/bin/bash

set -euxo pipefail

if [ -z "$1" ]; then
    echo "USAGE: app-timezone TIMEZONE"
    exit 1
fi

app-install tzdata
unlink /etc/localtime || true
cp /usr/share/zoneinfo/$1 /etc/localtime
echo "$1" > /etc/timezone
app-remove tzdata

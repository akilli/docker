#!/bin/bash

_opt=""

if [ "$DOCKER_TAG" == "next" ]; then
    _opt="--build-arg TAR=https://downloads.php.net/~carusogabriel/php-8.0.0alpha2.tar.xz"
fi

echo "docker build ${_opt} -f ${DOCKERFILE_PATH} -t ${IMAGE_NAME} ."
docker build ${_opt} -f ${DOCKERFILE_PATH} -t ${IMAGE_NAME} .
